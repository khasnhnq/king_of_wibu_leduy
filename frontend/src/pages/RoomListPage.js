import React from 'react';
import '../assets/css/RoomList.css';  // CSS cho trang danh sách phòng

const RoomListPage = () => {
  return (
    <div>
      <h1>Room List</h1>
      <p>Browse through the list of available rooms for your stay.</p>
    </div>
  );
};

export default RoomListPage;
