import React from 'react';
import '../assets/css/Register.css';  // CSS cho trang đăng ký

const RegisterPage = () => {
  return (
    <div>
      <h1>Register Page</h1>
      <p>Create a new account by filling in the form below.</p>
      {/* Bạn có thể thêm form đăng ký ở đây */}
    </div>
  );
};

export default RegisterPage;
